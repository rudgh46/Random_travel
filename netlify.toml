[build]
  publish = "."
  functions = "netlify/functions"

# 프론트에서 /api/tago 로 호출하면 함수로 연결
[[redirects]]
  from = "/api/tago"
  to = "/.netlify/functions/tago"
  status = 200

# 뽑기 횟수 카운터
[[redirects]]
  from = "/api/count"
  to = "/.netlify/functions/count"
  status = 200
